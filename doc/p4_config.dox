/** \file p4_config.dox
 * \brief Configuration dictionary for p4Reduce.
 * \author Jared R. Males
 */

/** \addtogroup p4reduce_user_guide
 * @{
 *
 * \section p4_configuration_dictionary p4Reduce configuration dictionary
 *
 * This dictionary adds P4 settings to the shared \ref common_configuration_dictionary
 * "common configuration dictionary". Long options use `--dotted.key value`; configuration files require a bare key
 * under `[section]`, as described in the shared dictionary. The key/value columns use dotted shorthand only to keep
 * the tables compact. The FITS-keyword column names the card written to final and saved
 * PSF-subtracted products. Keys containing spaces are emitted automatically as `HIERARCH` cards.
 * Frozen-model settings and their product-specific provenance are documented on the
 * \ref p4_psf_user_guide "Frozen-Model PSF Responses" page.
 *
 * \section p4_app_dictionary Application mode and scope
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--adi.excludeMethod` | `adi.excludeMethod=imno` | `HIERARCH P4 ADI EXCLUDE METHOD` | `none`, `pixel`, `angle`, or `imno` | `none` | Select target-frame reference exclusion. `none` preserves the in-sample model. Non-`none` methods exclude every candidate at or inside `adi.minDPx`, including the target itself at zero; `p4.exclusionSolver` chooses how the retained fit is calculated. |
 * | `--adi.minDPx` | `adi.minDPx=0` | `HIERARCH P4 ADI MIN DPX` | finite nonnegative real | `0` | Inclusive exclusion threshold in pixels at the annulus inner radius (`pixel`), degrees (`angle`), or temporal-row index (`imno`). |
 * | `--p4.exclusionSolver` | `p4.exclusionSolver=factorDowndateExact` | `HIERARCH P4 EXCLUSION SOLVER` | `explicitRefit` or `factorDowndateExact` | `explicitRefit` | Select the direct retained-row oracle or exact deletion from one complete safely represented base factor. A non-`none` `adi.excludeMethod` is required. |
 * | `--p4.deletionBackend` | `p4.deletionBackend=rankOneSecular` | `HIERARCH P4 DELETION BACKEND` | `leadingCovariance` or `rankOneSecular` | `leadingCovariance` | Select the mxlib factor-deletion implementation. `leadingCovariance` is dense and supports one or more deleted rows. `rankOneSecular` requires `factorDowndateExact` and exactly one deleted row for every active target. |
 * | `--mode` | `mode=basic` | — | `basic` or `normal` | `basic` | Exact aliases for one configured, target-only P4 reduction. |
 * | `--showTiming` | `showTiming=true` | — | bool | `false` | Print the completed P4 reduction timing report to standard output. |
 *
 * `p4Reduce` currently rejects RDI configuration and does not provide saved-product `postprocess`. Its external grid
 * scheduling is deliberately not an application mode: run separate fully specified configurations instead.
 *
 * \section p4_geometry_dictionary Search annuli and output support
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--geom.minRadius` | `geom.minRadius=3.5,6,8` | `HIERARCH P4 MIN RADIUS` | strictly ordered real pixels | required | Inclusive inner radii of P4 search annuli. |
 * | `--geom.maxRadius` | `geom.maxRadius=6,8,10` | `HIERARCH P4 MAX RADIUS` | matching ordered real-pixel vector | required | Exclusive outer radii. Annuli may not overlap. |
 *
 * Both geometry vectors and every P4 model setting may be omitted when `preProcess.only=true`; that mode stops after
 * shared preprocessing and never constructs P4 regions.
 *
 * P4 rejects geometry whose mapped detector-frame predictor interpolation footprint crosses an image edge. To retain
 * science over `[r0,r1)`, calculate halo annuli beyond both boundaries, then interpret or crop the desired science
 * interval after finalization. Merely widening the first science annulus changes its predictor geometry; a separate
 * support annulus preserves the core model.
 *
 * \section p4_model_dictionary Predictor region and PCA modes
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--p4.numberImages` | `p4.numberImages=1` | `HIERARCH P4 NUMBER IMAGES` | nonnegative integer | `0` | Append `N` nearest qualifying earlier and `N` nearest qualifying later physical-PSF-disk blocks to each predictor row. At endpoints, extra qualifying images from the available side replace missing blocks. Each side must rotate by at least `2 * p4.psfRadius` at the annulus mean radius. P4 lowers an unattainable request to the greatest positive structurally usable radius, or uses same-image predictors when none exists. |
 * | `--p4.modeFractions` | `p4.modeFractions=0.01,0.03,...` | `HIERARCH P4 MODE FRACTIONS` | finite, strictly increasing values in `(0,1]` | required | Requested PCA fractions. Each becomes `floor(fraction * min(T,K))`; zero or duplicate realized counts are rejected. |
 * | `--p4.orDeltaRadiusInner` | `p4.orDeltaRadiusInner=pixels` | `HIERARCH P4 OR DELTA RADIUS INNER` | positive real pixels | required | Inward radial extent of the local annular predictor wedge. |
 * | `--p4.orDeltaRadiusOuter` | `p4.orDeltaRadiusOuter=pixels` | `HIERARCH P4 OR DELTA RADIUS OUTER` | positive real pixels | required | Outward radial extent of that wedge. |
 * | `--p4.orArcHalfWidth` | `p4.orArcHalfWidth=pixels` | `HIERARCH P4 OR ARC HALF WIDTH` | nonnegative real arc length | required | Azimuthal half-width of the predictor wedge, expressed as arc length at the region radius; `0` uses only `orMaxHalfAngle`. |
 * | `--p4.orMaxHalfAngle` | `p4.orMaxHalfAngle=degrees` | `HIERARCH P4 OR MAX HALF ANGLE` | real degrees in `(0,180]` | required | Cap on the predictor wedge's angular half-width; `180` permits the full 360-degree annulus. |
 * | `--p4.rankTolerance` | `p4.rankTolerance=1e-12` | `HIERARCH P4 RANK TOLERANCE` | real in `[0,1)` | `1e-12` | Relative rank criterion: a mode is retained only when `lambda > tolerance * lambdaMax`. `0` accepts all positive returned eigenvalues, including roundoff-positive nulls. |
 *
 * For a positive `p4.numberImages`, `T` is the number of retained central targets in that annulus after temporal
 * neighbor selection, and `K` is the original OR predictor count plus `2 * p4.numberImages` physical-PSF-disk blocks.
 *
 * P4 regresses one fixed detector-pixel time series on its local OR without implicit centering or an intercept. The
 * \ref p4_rotated_user_guide "rotated-frame negative-result page" separately documents an experimental configuration
 * retained only as a numerical reference.
 *
 * P4 automatically uses the smaller temporal or predictor Gram formulation and performs the normal-equation work in
 * double precision. The Gram choice is not a configurable backend. Exact target exclusion requires detector-frame
 * regression with `p4.numberImages=0`; it is applied consistently to ordinary, pixel-local, and frozen-PSF fits.
 * `p4.deletionBackend` applies only to `p4.exclusionSolver=factorDowndateExact`; selecting `rankOneSecular` with the
 * explicit solver is rejected rather than ignored. P4 checks the realized compact exclusion pattern before
 * regression. With `rankOneSecular`, each target which retains any reference rows must delete exactly one row. A
 * target whose exclusion removes every row retains the ordinary rank-zero invalid result without invoking mxlib.
 * Recoverable structured-solver numerical failures, including positive LAPACK convergence statuses, trigger one
 * explicit refit of the complete search-pixel time series, so a search pixel never mixes structured and direct
 * target rows. Negative LAPACK statuses remain hard contract failures. Unsupported multirow structured exclusions
 * are configuration errors and never silently select the dense backend.
 *
 * \section p4_memory_dictionary Memory and worker management
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--p4.memoryFraction` | `p4.memoryFraction=0.8` | `HIERARCH P4 MEMORY FRACTION` | real in `[0,1]` | `0.8` | Fraction of Linux `MemAvailable` usable by future P4 allocations. P4 reduces each annulus's worker count when its compact retained storage plus estimated private workspaces would exceed this budget. `0` disables automatic discovery and limiting. |
 *
 * With a configured final combination and `output.outputPSFSub=false`, residuals are retained only for owned search
 * pixels. One full science/validity cube pair is materialized for each output mode, finalized, and released before the
 * next pair is allocated. `combine.method=none` and `output.outputPSFSub=true` retain the full residual and validity
 * cubes because those products are explicitly requested. The per-frame products and optional weights file are grouped
 * in the resolved final image's `_outputs` directory. P4 records the available-memory snapshot, selected budget,
 * compact-storage and materialization estimates, per-annulus worker estimate, maximum worker count, and effective
 * worker count in `HIERARCH P4 ...` FITS cards. `OMP_NUM_THREADS` remains a valid upper limit and is never increased.
 * A frozen response under target exclusion retains a distinct local response for every target frame. P4 selects the
 * number of output modes held simultaneously from the same budget, reruns the immutable held-out fit for each mode
 * batch, and lowers the response-fit and reconstruction worker counts independently. Thus a smaller budget can add
 * mode-batch passes but never substitutes shared in-sample coefficients.
 *
 * \section p4_exclusion_dictionary Signal exclusion
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--p4.psfRadius` | `p4.psfRadius=pixels` | `HIERARCH P4 PSF RADIUS` | positive real pixels | required | Physical central radius excluded from predictors. |
 * | `--p4.exclusionRadiusBuffer` | `p4.exclusionRadiusBuffer=0.5` | `HIERARCH P4 EXCLUSION RADIUS BUFFER` | nonnegative real pixels | required | Explicit additional radius applied to the physical exclusion. `0.5` reproduces the prototype's effective buffer. |
 * | `--p4.exclusionPolicy` | `p4.exclusionPolicy=sampleCenter` | `HIERARCH P4 EXCLUSION POLICY` | `sampleCenter` or `kernelSupport` | required | Defines how the buffered exclusion is tested. |
 *
 * `sampleCenter` rejects a predictor sample when its interpolated sample center lies inside the buffered disk. It is the
 * closest match to the prototype's center-only mask. `kernelSupport` rejects a predictor column when any pixel in its
 * nominal cubic-interpolation support lies inside the disk. Thus `kernelSupport` is more conservative near the
 * boundary, but it is not simply a larger `psfRadius`: the exact rejection depends on fractional location and the
 * orientation of the four-by-four detector footprint.
 *
 * \section p4_validity_dictionary Masks, rank, and invalid local fits
 *
 * Common input masks are authoritative. A local detector fit becomes invalid when its target or predictor support
 * touches a masked pixel.
 *
 * Numerical-rank failure invalidates only the unsupported output planes. P4 carries all invalidity in a separate
 * validity cube through the applicable final-processing operations; invalid values become NaN only after final
 * arithmetic. Nonfinite target input remains a hard reduction error.
 *
 * \section p4_diagnostics_dictionary Diagnostics and progress
 *
 * | CLI | Config key/value shorthand | FITS keyword | Type and limits | Default | Meaning |
 * |---|---|---|---|---|---|
 * | `--p4.writeDiagnostics` | `p4.writeDiagnostics=true` | `HIERARCH P4 WRITE DIAGNOSTICS` | bool | `false` | Writes ownership, validity, canonical-region, rank, timing, and summary FITS diagnostics. Every diagnostic carries `HIERARCH P4 FRAME` provenance. |
 * | `--p4.diagnosticDirectory` | `p4.diagnosticDirectory=/path` | — | writable directory | `.` | `.` groups diagnostics in the resolved final image's `_outputs` directory. Another value explicitly overrides that destination; missing parent directories are created automatically. |
 *
 * Geometry construction reports each annulus's search-pixel and retained predictor count. A memory line then reports
 * its estimated private MiB per worker and effective/pre-limit worker counts. Regression status reports the active
 * annulus, completed/total local fits, rate, local ETA, and cumulative overall progress to
 * standard error. `p4Validity_*.fits` is in detector coordinates. Final and PSF-subtracted FITS products record
 * `HIERARCH P4 FRAME = 'detector'`.
 * `p4Timing.fits` schema 6 has one row, in seconds: geometry elapsed, regression elapsed, total sampling, same-image
 * target/OR sampling, additional-image PSF-disk sampling, Gram-construction, eigensolve/rank-selection, reusable
 * base-factor construction, row deletion, explicit oracle fallback, and projection/residual aggregate OpenMP-worker
 * time. Base-factor and row-deletion time are subdivisions of the eigensolve aggregate. Explicit-fallback time
 * measures the complete direct-oracle retry and therefore overlaps its phase-specific Gram, eigensolve, and
 * projection totals; these diagnostic subfields must not be added to the aggregate total. Schema 7 appends aggregate
 * local-PSF worker time when `p4.psfFile` is set. Schema 8 appends elapsed final-field reconstruction/product time
 * when either `p4.outputPSFModels=true` or `p4.psfFilter=true`.
 * Worker totals can exceed elapsed time when local fits run concurrently; `HIERARCH P4 TIMING SCHEMA` and
 * `HIERARCH P4 TIMING COLUMNS` define the persisted layout.
 * `p4RegionSummary.fits` schema 2 adds `deletionSolverFallbackPixelCount` beside the existing rank-boundary and
 * factor-validation search-pixel counts. The final product header records the same per-annulus values in
 * `HIERARCH P4 DELETION SOLVER FALLBACK PIXELS`.
 *
 * @}
 */
